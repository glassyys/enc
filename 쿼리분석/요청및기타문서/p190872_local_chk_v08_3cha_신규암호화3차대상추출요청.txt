아래내용으로 소스생성요청
p190872_local_chk_v08_gm.py 참고하여 p190872_local_chk_v08_3cha.py 파일생성

1. 파라미터 구조
<스키마.검색기준테이블> 
<스키마.검색기준소스테이블> 
[--where <old/new/all>
- --db: DB 적재 활성화 여부 (선택)
- --conf: mysql.conf 경로 (선택)

2. 기준칼럼정보추출
검색기준테이블에서 칼럼리스트를
where 기준으로 기준칼럼추출 
--where old 이면 "where tobe_enc_key <> '' and asis_enc_yn = 'Y'"
--where new 이면 "where tobe_enc_key <> '' and asis_enc_yn = 'N'"
--where all 이면 "where tobe_enc_key <> '' 조건으로 조회하도록.

2. 칼럼기준 소스검색
검색기준소스테이블의 local_file의 소스에서 
query_text 자료추출하여 기준칼럼추출이 있는 행을 있는 만큼 조회하고
소스전체기준 라인과 함께 행내용추출하고
code 명령을 실행할수 있는 vscode_open_cmd
칼럼과 matched_line 추출하여 파일을 생성

3. 결과테이블생성 및 등록
1) 테이블은 p190872_ 로시작하고 3차_대상이란 의미단어를 끝에 추가하여 파일명생성
2) 등록테이블레이아웃은
검색기준테이블항목 뒤에
column_name,
tobe_enc_key,
Converted tobe_enc_key (e.g. e1),
line_number,
vscode_open_cmd,
matched_line

--2차수정요청
db_name과 tbl_name 때문에 중복된자료가 나오는 것으로보이는데
중복제거된 column_name 기준으로
local_file의 내용을 조회하면서
아래 레이아웃대로만 추출되도록 하고
id -- 검색기준소스테이블
mid -- 검색기준소스테이블
column_name,
tobe_enc_key,
Converted tobe_enc_key (e.g. e1),
line_number,
vscode_open_cmd,
matched_line

아래 값들 중복은 발생하지 않게 수정부탁
column_name,
tobe_enc_key,
Converted tobe_enc_key (e.g. e1),
line_number,
vscode_open_cmd,
matched_line

그리고
추가로 파일생성을 아래형식대로 하되 
match_str 은 테이블 대신 column_name 으로 추출되도록 수정해서 처리하고
실행시 화면에도 내용을 출력하도록 수정요청합니다.

match_str = "[매칭] %s %s (테이블: %s)" % (vscode_cmd, orig_col_name, assoc_tables_str)
content_str = "[내용] %s" % l_val.strip()
vscode_open_cmd = "[code] ...

--3차수정요청
local_file 기준으로 작업진행되는 상황을 화면 출력하도록 수정부탁
local_file 은 검색기준테이블 column_name으로 조회시작시 한번만 출력

--4차수정요청
검색기준테이블 자료가 없으면
column_name,tobe_enc_keys, conv_tobe_enc_key, matched_line는 '' 처리하고
line_number 1행 기준으로 vscode_open_cmd 값으로 등록하도록 수정
화면출력은 상위 10개 처리에 대해서만 하도록 수정하고 파일생성은 그대로 처리하도록 수정

--5차수정요청
1. 검색기준소스테이블에서 column_name 매칭되는 자료가 없더라고
id값 기준 1개의 행은 아래 기준값대로 결과테이블 생성되도록 수정
column_name,tobe_enc_keys, conv_tobe_enc_key, matched_line는 '' 처리하고
line_number 1행 기준으로 vscode_open_cmd 값으로 등록하도록 수정

2. 화면출력 [진행] 부분에 검색기준테이블의 id값도 추가료 표시하도록 수정

--6차수정요청
1. vscode_open_cmd 값 생성시 "[code]" 값은 표시 안하도록 수정
2. 타겟테이블에 검색기준소스테이블의 source_file 칼럼추가 적재처리로 수정

--7차수정요청
타겟테이블명을 검색기준소스테이블명_work 로 테이블명 변경하여 수정
-------


20260630
Packet sequence number wrong - got 1 expected 2
오류에 대한 조치
성공




--------------------------------
--------------------------------
--------------------------------
--------------------------------

---- 아래요청은 잘 실행이 안되어 다시 요청처리해야 합니다.
테이블 칼럼 매칭자료 추출용


p190872_local_chk_v08_3cha.py 소스수정요청
다시 소스 수정요청합니다
기존 요청은 무시하고
 로직에서
기존테이블 생성하는 부분은 work_02 로끝나는 테이블명으로 유지하고
검색기준테이블에서 tbl_name, column_name 파일생성하고 등록하는 내용과
검색기준소스테이블의 source_file의 소스에서
sql문 query_text를 추출해서 파일 생성하는 내용으로 수정
p190872_local_chk_v08_3cha_add.py 파일로 생성되도록

query_text를 추출하는 로직은 아래 소스 참고합니다
Sql v12 full new 02 local.py

query_text 등록하는 테이블은 별도로 파일생성하고 테이블도 생성해서 등록하고
소스에대한 테이블리니지 즉 소스와 타겟테이블을 정리한 리니지 파일과 테이블도 생성해서 수정부탁


아래 "[Sql v12 full new 02 local.py]" 응용하여 
검색기준테이블의 tbl_name과 column_name 기준으로 
소스내 query_text 내용중 일치하는 자료를 추가파일생성하고
현재 생성하는 테이블레이아웃대로 가능한자료추출하고 
테이블명 칼럼을 추가로 처리하여 테이블등록하도록 수정하여
p190872_local_chk_v09_3cha.py 파일생성요청
테이블 등록방식은 있으면 mid기준 삭제후 처리 없으면 생성하는 로직 유지합니다

p190872_local_chk_v08_3cha.py 소스수정요청

아래 "[sql_v12_full_new_02_local.py]" 응용하여 
검색기준테이블의 tbl_name과 column_name 기준으로 
소스내 query_text 내용중 일치하는 자료를 추가파일생성하고
현재 생성하는 테이블레이아웃대로 가능한자료추출하고 
테이블명 칼럼을 추가로 처리하여 테이블등록하도록 수정하여
p190872_local_chk_v09_3cha.py 파일생성요청
테이블 등록방식은 있으면 mid기준 삭제후 처리 없으면 생성하는 로직 유지합니다.


[Sql v12 full new 02 local.py]
# ─────────────────────────────────────────────────────────────
# 1) 실행 시 파라미터: 검색대상_디렉토리, 검색기준테이블, [옵션]
# 2) 서버 MySQL 에서 <검색기준테이블> 전체 데이터 조회
#    - 조회 칼럼: db_name, tbl_name, column_name, tobe_enc_key, tobe_enc_rsn
# 3) 조회된 (tbl_name, column_name) 쌍을 검색 기준으로 메모리에 저장
# 4) 검색대상_디렉토리 하위 소스 파일 (.sql, .hql, .uld, .ld, .sh) 전체 탐색
# 5) 각 소스 파일에서 쿼리 단위로 추출 (주석 제거 후 검색)
# 6) 추출한 쿼리의 source/target 테이블 중 tbl_name 과 일치하는 항목 확인
# 7) tbl_name 일치 + column_name 이 해당 쿼리에 포함(\b단어\b 완전일치) 시 매칭
# 8) 매칭 결과를 스크립트 하위 out/ 디렉토리에 CSV 로 저장

1차 보완요청
해당소스내에 쿼리 모두에서 매칭되는 tbl_name이 있고
column_name이 있는 경우 추출하는 것이 맞는지?
요청 내용은 source_file에서 각 소스내부 쿼리기준
검색시 기준테이블의 tbl_name 과 column_name에 대한 전체자료를
순환하여 검색하여 추출하고 다음 소스파일에 대한 진행을 해야 합니다.
tbl_name과 column_name 값도 화면출력에 같이 표시하는 부분추가해서 수정요청합니다.

너무 느려서 구조를 변경합니다.
먼저 source_file 기준 쿼리와 소스타겟 리니지 정보를 추출한 후
tbl_name과 column_name 매칭정보를 추출하도록 소스 수정

검색기준소스에서 query_text를 추출하고 파일로 저장하는 부분 추가하고
tbl_name과 column_name 매칭정보 생성하는 테이블을 등록하는 부분은 skip하도록 임시로 소스 수정하고
query_text가 포함된 자료 생성후 그 자료를 읽어서 다시 생성예정이니 그 부분고려하여 임시로 skip 처리 




p190872_local_chk_v08_3cha.py
소스를 column_name이 있는데 예외적으로 추출하지 않는 경우를
화면에 출력하도록 하고 별도 파일로 생성하도록 소스 수정
p190872_local_chk_v09_3cha.py으로 소스생성요청합니다

p190872_local_chk_v08_3cha.py
이 소스를 주석인경우만 추출하지 말고
나머지는 다 매칭자료 추출되도록
column_name기준으로 자료 추출하도록 수정하고
결과테이블 등록시 기존테이블명과 다르게 변경하고
혹시 제외되는 경우는 
화면 출력및 파일로 생성하도록 소스 수정요청합니다.
기존 화면출력부분은 변경하지 말고 수정요청합니다.
최종소스는
p190872_local_chk_v08_3cha_no_omit.py로 생성부탁


[매칭]자료 화면출력시 
code 명령 마지막에 ":라인번호" 추가해서 출력하고
vscode_open_cmd 출시 ":1" 이 아닌 ":line_number"로 추출하도록 수정요청합니다.

입력된 파라미터 <검색기준테이블> 조회후 요청한 조건에 의해
입력된 <분석대상_디렉토리> 하위 소스들에 대해 요청조건에 의한
tbl_name,cols 값이 포함된 행을 찾아서 파일생성후
테이블에 등록하는 내용으로 v12 full new 03 local.py  소스를 생성합니다.
[소스생성요청조건]
1. 입력된 <검색기준테이블> 기준으로 MySQL에서 데이터 조회
2. 조회된 컬럼 결과값중 tbl_name과 cols 값 리스트를 추출
3. 입력된 tbl_name과 cols기준으로  아래처럼 자료를 추출
<분석대상_디렉토리> 하위소스 내용의 query_text 추출내용 중
tbl_name이 있고 그리고
cols 내용중 "col_01:k1,col_bb:k2" 이라면
첫번째 "," 앞의 내용중 ":"앞의 "col_01" 이 있는 경우 소스기준 라인수 포함 해당라인을 추출
하고 "," 기준 다음 내용에 대해서도 같은 작업을 하며 데이터가 있을때까지 순환작업을 합니다.
cols 항목이 ,로 분리되어 "칼럼명:키값" 구조로 되어 있는데 ","로 분리된 내용이 여러개가 있을수 있습니다.
4. 추출된 행의 값과 라인수 추출
3번조건에 일치하는 행의 내용과 전체소스기준 라인수를 추출해서
<검색기준테이블> 칼럼들 포함해서 입력테이블값 뒤에 "라인수"와 "추출된행"을 추가하여
파일로 생성후 새로운 MySQL테이블에 등록

5. 실제 실행방식은 아래와 같습니다.
sql v12 full new 03 local.py <분석대상_디렉토리> <검색기준테이블> [--db] [--conf mysql.conf 경로]
참고소스에 있는
테이블을 기본적으로 조회해야 하는 내용이라 [--db] [--mode SIMPLE|DETAIL] 파라미터 기능은 생략합니다.

6. <검색기준테이블> 레이아웃 검색기준자료샘플내용.jpg 첨부이미지내용입니다.
db_name
tbl_name
operation
no
source_file
process_yn
process_desc
cols
enc_col_cnt
ins_cnt
sel_cnt

7. 소스내용과 실행예시 등을 정리해서 주석에 추가

8. 참고소스:이전에 첨부했던 아래 소스내용의 로직을 참고
sql_find_v001_01.py 
sql_find_v001_01_local.py
sql_v12_full_new_02.py




추출된 행의 수정코드행 추가요청


2. 기존코드를 수정코드 형식으로 변경된 내용으로 변환해서 행을 추가
1) 수정코드의 의미중 'e1' 코드 값기준
col_01:key1 이면 'e1'값
col_bb:key2 이면 'e2'값 사용으로 key2 값의 숫자기준입니다.

2) 대소문자의 경우 변환없기 기존코드유지

3) 기존코드의 칼럼명을 "col_01:k1,col_bb:k2" 의  col_01 이나 col_bb과 일치하는 경우 변환처리


소스 요건 확인 및 수정요청
1. "6) 매칭 조건 (AND 조건):" 조건 결과가 아래와 같은지 확인요청

예시가
tbl01
"col_01:k1,col_bb:k2"
인경우 쿼리가

아래의 CASE1, CASE2 경우가 대상으로 추출되는지?

CASE1)
insert into tbl01 
select 
max(col_01) as col_01
from tbl02
;

추출행 결과 "소스라인번호", "max(col_01) as col_01"

CASE2) query_text
insert overite tbl99
select 
nvl(col_bb) as col_bb
from tbl01
;

추출행 결과 "소스라인번호", "nvl(col_bb) as col_bb"

2. 1번결과가 맞는지 확인하고 맞지않으면 수정
3. cols 값을 화면에 출력요청
4. <검색기준테이블> 행유지 
<검색기준테이블>에 대한 추출되는 행이 없더라고 결과테이블에는 NULL이나 ''값으로 등록요청
5.  cols 에 존재하는 ","구분 기준 분리된 값만큼 테이블과 칼럼 조건에 맞는 자료 추출
이런 내용 적용하여 소스 수정 요청합니다.

###################################

sql_v12_full_new_03_local.py

위 소스에 대해
아래내용이 맞는지 확인하고
아니면 다른로직 수정하지말고 보완 수정요청
tbl_name 값이 소스테이블이나 타겟테이블 목록에 있고 
cols 분리값이 기준칼럼값이 대소문자 구분없이 query_text에 포함된 행이 있으면
결과 추출되도록 수정

sql_v12_full_new_03_local.py
실행시 아래 오류가 나는 부분 해결한 수정버전
Packet sequence number wrong = got 1 expected 2

말씀하신 Packet sequence number wrong = got 1 expected 2 오류는 PyMySQL(또는 mysql-connector)을 사용할 때 이전 쿼리의 결과 셋을 완전히 읽어 들이지 않은 상태
(버퍼에 남은 데이터가 있는 상태)에서 다음 쿼리를 실행하려고 할 때 발생하는 전형적인 패킷 꼬임 현상 해결한 수정소스코드를 분석해서 패킷 꼬임 현상의 근본 원인을 파악하고 수정하겠습니다.Analyze the code for packet sequence issuesAnalyze the code for packet sequence issues코드는 문서로 받았으니 바로 분석해서 수정하겠습니다. 패킷 꼬임의 원인은 세 군데입니다.
원인 1 — load_search_pairs_from_db()에서 SHOW TABLES LIKE ... → SHOW COLUMNS FROM ... → SELECT ... 세 쿼리를 같은 커서로 연속 실행하면서 이전 결과셋이 완전히 소비되지 않을 때 발생
원인 2 — SHOW TABLES LIKE '%s' 에 % 포맷팅 사용 → PyMySQL이 파라미터로 오인해 이스케이프 오작동 가능
원인 3 — connector 모드에서 buffered=True 커서를 전체 공유하는 대신, 쿼리별로 커서를 새로 발급하지 않아 잔여 결과셋이 남음명령 4개 실행함, 파일 조회함, 파일 생성됨명령 4개 실행함, 파일 조회함, 파일 생성됨v003_local 수정 내용 요약
패킷 꼬임의 근본 원인은 커서를 재사용하면서 이전 결과셋이 소비되지 않은 채 다음 쿼리가 실행되는 것입니다. 세 개의 헬퍼 함수로 이를 완전히 차단했습니다.

추가된 헬퍼 3종 (96~147행)
함수역할_new_cursor(conn)드라이버별 올바른 커서 생성 — connector는 buffered=True 강제_exec_query(conn, sql, params)새 커서로 실행 → fetchall() → 즉시 cursor.close()_exec_many(conn, sql, data)새 커서로 executemany() → 즉시 cursor.close()
pymysql에서 cursor.close()를 호출하면 서버 측 잔여 패킷을 강제로 소비(drain)하기 때문에 다음 쿼리 실행 전 소켓이 항상 깨끗한 상태가 됩니다.

변경된 함수별 수정 포인트
load_search_pairs_from_db() — 가장 큰 원인이었던 곳

기존: 하나의 커서로 SHOW TABLES → SHOW COLUMNS → SELECT 를 순서대로 실행
수정: 세 쿼리를 각각 _exec_query() 로 분리 → 매번 새 커서, 즉시 닫기
추가: SHOW TABLES LIKE '%s' % ref_table (문자열 포맷팅) → execute(sql, (bare_table,)) 파라미터 바인딩으로 변경 (pymysql이 %를 파라미터 마커로 오인하는 문제 동시 해결)

db_insert_col_match_all() / db_insert_sql_all()

기존: 단일 커서로 DROP → CREATE → 500건 청크 루프 전체 처리
수정: DDL은 _exec_query(), 청크 INSERT는 _exec_many() — 매 호출마다 새 커서, 즉시 닫기

제거된 것

_get_cursor() 함수 전체 제거 (커서를 오래 유지하는 패턴의 원천)





--where  조건을 아래와 같이 수정한 전체소스
--where old 이면 "where tobe_enc_key <> '' and asis_enc_yn = 'Y'"
--where new 이면 "where tobe_enc_key <> '' and asis_enc_yn = 'N'"
옵션이 없으면 "where tobe_enc_key <> ''" 조건으로 조회하도록 변경요청합니다.

② load_search_pairs_from_db() — WHERE 절 동적 적용
where_opt=None  → WHERE `tobe_enc_key` <> ''
where_opt='OLD' → WHERE `tobe_enc_key` <> '' AND `asis_enc_yn` = 'Y'
where_opt='NEW' → WHERE `tobe_enc_key` <> '' AND `asis_enc_yn` = 'N'

그럼 추출된
_sql 로 끝나는 테이블에서
같은소스 내에 query_text 내용에서
"검색기준 쌍 수"에 해당하는 내용중
source 와 target중 tbl_name과 일치하고
이 조건을 만족하면서 
동일한 소스내 query_text 내용에서
기준칼럼(column_name)이 있으면
해당 소스정보와 "검색기준 쌍 수"와
전체소스기준 매칭라인의 라인번호와 내용을 추출하고
vs코드에서 조회가능하도록
"code -g dir_file:1" 값도 항목 추가하여 
파일생성 및 테이블 등록하도록 소스수정한 전체소스 부탁







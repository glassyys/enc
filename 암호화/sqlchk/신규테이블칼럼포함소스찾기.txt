첨부한 sql_find_v002.py 과 sql_v12_full_emrput.py 소스 응용하여 신규소스 sql_v12_full_new.py파일로 생성요청합니다.

sql_v12_full_new.py 로직흐름과 출력스타일과 테이블 등록방법등 유지하면서 검색항목의 경우 sql_find_v002.py 내용 참조하면서 아래 내용 적용하여 수정

1. 파라미터는 [검색대상_디렉토리] [경로포함_CSV파일명] [--mode SIMPLE] [--db] [--conf mysql.conf 경로]
2. 경로포함_CSV파일명내 헤더는 제외하고  "," 기준 첫번째항목은 테이블이고 두번째 항목은 칼럼명으로 리스트 추출후 
   실행디렉토리 하위 in 디렉토리에 검색용파일 생성
3. sql_v12_full_emrput.py 로직에서 검색대상_디렉토리 하위의 소스내부 
   쿼리추출한 후 쿼리기준으로 해당쿼리내에 테이블과 칼럼이 모두 존재하면
   CRUD구분코드(sql_type)와 해당쿼리의 target_table 정보도 추출하고 를 추가하여
   CRUD구분코드(sql_type), 칼럼존재라인, 테이블, 칼럼명 추가하여 기존 항목 유지가능한내용 유지하여 
   추가요창한 항목포함 csv결과파일로 생성후
4. 기존 테이블  등록 테이블 (리니지)과 등록 테이블(쿼리) 위에 추가로 3번에서 생성한 파일을 신규테이블로 로드
5. 변경한 내용도 이력으로 주석처리 작성


table_name, column_name( 첫행헤더명고정,이후데이터) 로 처리해서 적용

오후 3:07
확인했습니다. 나머지 2개 질문도 답변 부탁드립니다.

질문: 3번 항목 - '쿼리 내에 테이블과 칼럼이 모두 존재'의 의미는?
답변: source/target 테이블 중 하나라도 일치 + 칼럼명 포함 시

질문: 4번 신규테이블의 drop/recreate 방식은?
답변: 기존 리니지/쿼리 테이블과 동일하게 DROP→CREATE→INSERT

모든 요건이 확인됐습니다. 소스 생성을 시작합니다.명령 3개 실행함, 파일 조회함, 파일 생성됨명령 3개 실행함, 파일 조회함, 파일 생성됨정상 동작 확인됐습니다. 파일을 출력합니다.명령 실행함, 파일 읽음명령 실행함, 파일 읽음구현 내용을 정리하면 다음과 같습니다.
[신규1] 파라미터 변경
python3 sql_v12_full_new.py <검색대상_디렉토리> <경로포함_CSV파일명> [--mode SIMPLE|DETAIL] [--db] [--conf 경로]
[신규2] CSV 입력 처리

헤더 table_name, column_name 고정 인식 (대소문자·공백 무시)
중복 쌍 제거 후 실행 디렉토리 하위 in/ 폴더에 검색용 복사본 저장

[신규3] 테이블+칼럼 매칭 로직 (build_col_match_rows)

각 추출 쿼리의 source/target 테이블 중 하나라도 CSV의 table_name과 대소문자 무시 일치
동시에 쿼리 텍스트에 column_name이 \b단어\b 완전일치로 포함
match_type 컬럼으로 SOURCE/TARGET 구분, matched_line으로 칼럼이 나온 첫 번째 라인 기록

[신규4] 칼럼매칭 CSV 생성

파일명: {PROGRAM_NAME}_{last_dir}_col_match_{timestamp}.csv
컬럼: base_directory, file_name, dir_file, crud_type, sql_type, target_table, matched_table, matched_column, match_type, matched_line, op_dtm

[신규5] 칼럼매칭 DB 테이블 (DROP→CREATE→INSERT)

테이블명: {PROGRAM_NAME}_{last_dir}_{mode}_col_match
기존 리니지 테이블, 쿼리텍스트 테이블 등록 후 추가로 처리

